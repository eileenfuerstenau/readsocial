import SearchBar from './SearchBar'

export default {
  title: 'SearchBar',
  component: SearchBar,
}

const Template = args => <SearchBar> Read more </SearchBar>

export const Primary = Template.bind({})
